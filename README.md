Symposium event registration website with a simple and clean UI/UX.

Local chatbot setup:

1. Add your Gemini key to `.env` as `GEMINI_API_KEY="your_key_here"`.
2. Start the app with `vercel dev` so `/api/generate` works locally.
3. Restart the dev server after changing environment variables.
